import classes from './Settings.module.css';

const Settings = (props) =>
{
    return(
        <div className={classes.page__settings}>
            Settings
        </div>
    )
}
export default Settings;